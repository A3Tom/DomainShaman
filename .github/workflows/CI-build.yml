
name: CI
on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: Banksy
        shell: pwsh
        run: |
          $commitId = ${{ github.sha }}
          $nextVersion = .\version_chanter.ps1 $commitId

          echo Taggin complete
