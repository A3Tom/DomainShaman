
name: CI
on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: Banksy
        shell: pwsh
        run: |

          $nextVersion = .\version_chanter.ps1

          git config user.name github-actions
          git config user.email github-actions@github.com
          git tag -a "release/v$nextVersion" ${{ github.sha }} -m "Release v$nextVersion"
          git push --tag


      - name: Run a multi-line script
        run: |
          echo Taggin complete
